%h1
  Grupa wydarzeń:
  = topic.name

%h3= topic.description

= link_to 'Edytuj', edit_topic_path(topic), class: "btn btn-warning spacious"
= link_to 'Usuń', topic, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: "btn btn-danger"
= link_to 'Pokaż oś czasu', learning_path(topic), class: "btn btn-info"
= link_to 'Cofnij', topics_path, class: "btn btn-default spacious"
= link_to 'Nowe wydarzenie', new_topic_event_path(topic), remote: true, class: "btn btn-success spacious new-event"

%table.table.table-hover
  %thead
    %tr
      %th Początek
      %th Koniec
      %th Zdarzenie
      %th
  %tbody
    - topic.events.each do |event|
      %tr{id: event.id }
        %td= event.start_date
        %td= event.end_date
        %td= event.name
        %td
          %span= link_to 'Pokaż', topic_event_path(topic, event), remote: true, class: "btn btn-primary show-event"
          %span= link_to 'Edytuj', edit_topic_event_path(topic, event), remote: true, class: "btn btn-warning edit-event"
          %span= link_to 'Usuń', topic_event_path(topic, event), :method => :delete, :data => { :confirm => 'Are you sure?' }, remote: true, class: "btn btn-danger delete-event"