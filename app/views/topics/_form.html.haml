= simple_form_for(topic) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
  .form-actions
    - if current_page?(controller: 'topics', action: 'new')
      = f.button :submit, class: "btn btn-success"
    -else
      = f.button :submit, class: "btn btn-warning"
